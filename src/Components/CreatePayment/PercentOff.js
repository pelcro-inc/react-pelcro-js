import React, { useContext } from "react";
import { store } from "../CheckoutForm/CheckoutFormContainer";

export const PercentOff = () => {
  //   const { state } = useContext(store);

  //   return <span>{state.percentOff}</span>;
  return "";
};
